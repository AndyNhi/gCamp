- unless notice.nil?
  %p.alert.alert-danger= notice

.page-header
  .pull-right
    = link_to 'Create Task', new_task_path, :class => 'btn btn-default'
    = link_to 'Download CSV', tasks_path(format: :csv), class: 'btn btn-default'
  %h1 Listing Task

%table.table
  %tr
    %th Description
    %th Due Date
    %th Complete Task?
    %th

  - @tasks.each do |task|
    %tr
      %td= task.description
      - if task.due_date.nil?
        %td Infinite
      - else
        %td= task.due_date.strftime("%B %e, %Y")
      %td
        = form_for(task, html: {class: "form-horizontal"}) do |f|
          = f.check_box :complete
          = f.submit :class => 'btn btn-default btn-sm'
      %td
        .pull-right
          = link_to 'Show', task, class: 'btn btn-primary'
          = link_to 'Edit', edit_task_path(task), class: 'btn btn-success'
          = link_to 'Destroy', task, class: 'btn btn-danger', :method => :delete, :data => { :confirm => 'Are you sure?' }
