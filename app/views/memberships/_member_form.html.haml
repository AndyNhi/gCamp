.table
  = form_for [@project, membership], html: {class: 'form-inline'}  do |f|
    .form-group.col-xs-1
      = membership.user.first_name
    .form-group.col-md-offset-1
      = link_to("< Remove Your Membership", project_membership_path(@project, membership), method: :delete, :data => { :confirm => 'Are you sure?' }, title: 'delete')
